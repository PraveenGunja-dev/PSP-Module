
  # DPR

  This is a code bundle for DPR. The original project is available at https://www.figma.com/design/V8xPzA99y53jehR59qfAdk/DPR.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  